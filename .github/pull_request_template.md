## What and why

<!-- What does this change, and what problem does it solve? Link the issue if there is one. -->

Fixes #

## How it was tested

<!-- Delete what doesn't apply. Note the Obsidian versions you ran against if it
     wasn't the default 1.1.8 + latest. -->

- [ ] `npm run lint` and `npm run check-types` pass
- [ ] `npm test` passes (on Linux: `xvfb-run -a npm test`)
- [ ] Added or updated an e2e spec in `test/specs/`
- [ ] Checked by hand in Obsidian

## Rendering changes

<!-- If this changes how callouts look, or touches the settings tab, attach a
     before/after. `npm run screenshots` regenerates the README images; the e2e
     run writes per-version renderings into test/screenshots/. Delete this
     section if the change isn't visual. -->

## Notes for the reviewer

<!-- Anything worth flagging: decisions you weren't sure about, follow-ups you
     deliberately left out, compatibility with older Obsidian versions. Delete if
     there's nothing. -->
